apiVersion: v1
kind: Service
metadata:
  name: chat-client
  namespace: chat-app
  labels:
    app.kubernetes.io/name: chat-client
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: chat-client
