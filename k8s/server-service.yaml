apiVersion: v1
kind: Service
metadata:
  name: chat-server
  namespace: chat-app
  labels:
    app.kubernetes.io/name: chat-server
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: chat-server
