apiVersion: v1
kind: Service
metadata:
  name: chat-server-nodeport
  namespace: chat-app
  labels:
    app.kubernetes.io/name: chat-server
    app.kubernetes.io/component: backend
spec:
  type: NodePort
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      nodePort: 30001
      protocol: TCP
  selector:
    app.kubernetes.io/name: chat-server
